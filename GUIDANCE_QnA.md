# 평가 대비 Q&A — 6개 핵심 기능 면접 답변

> 출처: `Team_Project_Evaluation_Guidance.pdf`
> 평가의 핵심은 *"게임이 돌아가느냐"가 아니라 "왜 그 자료구조/알고리즘을 썼는지 방어할 수 있느냐"* 입니다.
> 평가관이 강조한 세 가지: **Run it. Defend it. Compare it.** (실행 / 방어 / 비교)

---

## 0. 먼저 — "Guidance 적용"이 무슨 뜻인가

Guidance를 코드에 "적용"한다는 건 새 기능을 추가하라는 게 아닙니다. 이미 구현된 6개 기능을 **평가 기준에 맞게 설명·방어할 수 있게 정리**하는 것입니다. 구체적으로:

1. **6개 핵심 기능이 코드에 실제로 존재**하는가 (각 15점, 합 90점)
2. 각 기능마다 **어떤 자료구조/알고리즘**을 썼고 **그 파일·함수 위치**를 짚을 수 있는가
3. **대안과 비교**(복잡도 포함)해서 선택을 방어할 수 있는가
4. PPT / 주석이 **코드가 실제로 하는 일과 일치**하는가

배점 (총 200점):

| 항목 | 비중 |
|------|------|
| PPT & 알고리즘 제출 + AI+X Studio 호환성 | 30% |
| 6개 핵심 기능 (15점 × 6) | 15% |
| 알고리즘 비교 & 설명 | 10% |
| **인터뷰(면접)** | **25%** ← 가장 큼 |
| 라이브 게임 데모 | 20% |

인터뷰가 25%로 가장 큰데, 이 문서가 그 인터뷰 대비용입니다.

---

## 답변 템플릿 — 5단계 (PDF 8쪽)

평가관이 명시한 "좋은 답변" 형식. **한 단계짜리 답("스택 썼어요")보다 다섯 단계를 다 밟는 답이 만점에 가깝습니다.**

| 단계 | 내용 | 예시 (Undo) |
|------|------|------------|
| 1 | 자료구조/알고리즘을 한 문장으로 | "스택을 썼습니다." |
| 2 | 왜 그게 맞는지 | "Undo는 LIFO라 스택의 접근 패턴과 일치합니다." |
| 3 | 코드 위치를 짚기 | "`frame.py`의 `Frame` 클래스입니다." |
| 4 | 대안과 비교 | "큐였다면 FIFO라 행동을 잘못된 순서로 재생합니다." |
| 5 | 복잡도/한계 | "push/pop은 O(1), 메모리는 히스토리 길이에 비례합니다." |

---

# 1. Dungeon Map (던전 맵)

**자료구조: 2D 그리드 (`list[list[int]]`)**

### 5단계 답변

1. **무엇** — 맵을 2차원 정수 그리드(`list[list[int]]`)로 표현합니다. 각 정수는 타일 종류(바닥·벽·문·시작·도착)입니다.
2. **왜** — 던전이 격자 기반이고 좌표 `(x, y)`로 칸을 O(1)에 직접 접근해야 하기 때문입니다. `grid[y][x]` 한 번이면 타일을 읽습니다.
3. **어디** — `map.py`의 `Map` 클래스. 그리드는 `self.map`([map.py:135](map.py#L135)), 접근은 `get_tile`/`is_walkable`([map.py:145-158](map.py#L145-L158)), 절차적 생성은 `_generate`([map.py:173](map.py#L173))에서 방을 놓고(`_place_rooms`) L자 복도로 잇습니다(`_connect_rooms`).
4. **대안 비교** —
   - **인접 리스트(그래프)**: 칸 사이 연결을 명시적으로 저장. 격자처럼 "상하좌우 4방향"이 암묵적으로 정해진 경우엔 과하고 메모리만 더 씁니다. 좌표→칸 접근도 그리드가 O(1)로 더 빠릅니다.
   - **단일 1D 리스트 + 인덱스 계산**(`grid[y*W+x]`): 가능하지만 가독성이 떨어집니다. 2D 리스트가 코드가 더 읽힙니다.
5. **복잡도** — 칸 접근 O(1). 공간 O(W×H) = 80×50 = 4,000칸. 통행 가능 판정(`is_walkable`)도 타일 종류만 보면 되니 O(1).

> **예상 질문 (PDF 7쪽)**: "맵을 어떤 자료구조로 표현했나? 왜? 이동 가능한 경로는 어떻게 확인하나 — BFS/DFS와 비교."
> **답 요지**: 그리드 + O(1) 타일 조회로 이동 판정. BFS/DFS는 경로 *탐색*용이지 단일 칸 통행 판정용이 아님 — 적 AI 추적(아래 5번)에서 BFS를 따로 씁니다.

---

# 2. Undo System (실행 취소)

**자료구조: 스택 (LIFO)**

### 5단계 답변

1. **무엇** — 게임의 매 턴 상태(프레임)를 스택에 쌓습니다. Undo는 맨 위를 pop합니다.
2. **왜** — Undo는 본질적으로 LIFO입니다. 가장 최근 행동을 가장 먼저 되돌리므로 스택의 접근 패턴과 정확히 일치합니다.
3. **어디** — `frame.py`의 `Frame` 클래스. push는 `__init__`([frame.py:12](frame.py#L12)), pop은 `undo`([frame.py:17](frame.py#L17)). 최대 30프레임까지만 유지([frame.py:14](frame.py#L14)). 매 턴 새 프레임을 쌓는 호출은 `main.py`의 `turn_update` 끝의 `Frame(new_frame)`([main.py:76](main.py#L76)).
4. **대안 비교** — **큐(FIFO)**였다면 가장 *오래된* 상태부터 빠져나가 행동을 잘못된 순서로 재생하게 됩니다. Undo는 최신부터 되돌려야 하므로 큐는 부적합합니다.
5. **복잡도/한계** — push/pop 모두 O(1). 메모리는 히스토리 길이에 비례(여기선 30개로 상한). **trade-off**: 우리는 *전체 상태(full state)* 를 저장합니다 — 구현이 단순하고 어떤 변화든 정확히 복원되지만, 프레임당 메모리가 큽니다. *델타(delta, 바뀐 것만)* 저장은 메모리는 작지만 역연산 로직이 복잡해지고 버그가 나기 쉽습니다.

> **예상 질문 (PDF 7쪽)**: "왜 스택이 Undo에 잘 맞나? 전체 상태를 저장하나, 델타만 저장하나 — trade-off는?"

### ⭐ 우리 프로젝트만의 심화 답변 — 맵 공유 최적화

평가관이 "단순히 돌아간다고 끝이 아니다"라고 했으므로, 이 부분이 가산점 포인트입니다.

- **문제**: 처음엔 매 턴 `copy.deepcopy(frame)`으로 프레임 전체를 복사했는데, 프레임 안에 80×50=4,000칸짜리 맵 그리드가 들어 있어 **한 층 안에서는 절대 안 바뀌는 맵을 매 턴 4,000칸씩 복사**하고 있었습니다 — 순수 낭비.
- **개선**: 맵은 한 층 동안 불변이므로 모든 프레임이 **하나의 맵 객체를 공유**하게 하고, deepcopy 대상에서 맵만 제외했습니다. 매 턴 바뀌는 `player`/`enemies`/`items`만 깊은 복사합니다 ([main.py:24-34](main.py#L24-L34)).
- **주의점 (이게 핵심)**: 맵을 공유하면 `load_next_floor`([map.py:429](map.py#L429))가 그 *공유* 객체를 제자리에서 변형(`floor += 1`, `_generate()`)하므로, 그대로 두면 **층을 넘은 뒤 undo하면 옛 프레임이 이미 새 층으로 바뀐 맵을 가리켜 깨집니다.** 그래서 층 전환 턴에는 `Frame.reset_history()`([frame.py:24](frame.py#L24))로 undo 스택을 새 층 프레임 1개로 비웁니다 ([main.py:79-81](main.py#L79-L81)).
- **이게 곧 full-state vs delta의 실전 trade-off 사례** — "전체를 복사하면 단순하지만 무겁다 → 안 바뀌는 부분(맵)은 공유로 빼서 비용을 줄였고, 그 대가로 공유 객체의 생명주기(층 경계)를 직접 관리해야 했다"고 답하면 5단계를 다 밟는 모범 답변이 됩니다.

---

# 3. Turn Management (턴 관리)

**자료구조: 순차 처리 (이벤트 → 플레이어 → 모든 적 순서)**

### 5단계 답변

1. **무엇** — 한 턴은 "플레이어 행동 → 모든 적 행동" 순서로 처리됩니다. 적들은 리스트를 순회하며 차례로 행동합니다.
2. **왜** — 우리 게임은 플레이어 입력이 있을 때만 한 턴이 진행되는 입력 구동(turn-based) 방식이라, 플레이어를 먼저 처리한 뒤 적 리스트를 한 번 순회하면 충분합니다.
3. **어디** — `main.py`의 `turn_update`([main.py:18](main.py#L18)). 플레이어 행동 처리 후 `for enemy in enemies: enemy.update(...)`([main.py:62-65](main.py#L62-L65))로 모든 적이 행동.
4. **대안 비교** —
   - **단순 큐(FIFO)**: 모든 캐릭터가 같은 속도면 리스트 순회로 충분 — 큐를 명시적으로 둘 필요가 없습니다.
   - **우선순위 큐(힙)**: 캐릭터마다 속도가 다르면 "다음에 행동할 차례"를 속도 기준으로 뽑아야 하므로 우선순위 큐(힙)가 맞습니다. 우리 `FastEnemy`는 한 턴에 2번 움직이는데([enemy.py:243-251](enemy.py#L243-L251)), 이건 "행동 시점을 힙으로 스케줄링"하는 대신 "한 턴에 루프를 2번"으로 단순화한 것입니다.
5. **복잡도** — 적 N마리 순회 O(N). 우선순위 큐로 속도별 스케줄링을 한다면 한 번 뽑을 때 O(log N).

> **예상 질문 (PDF 7쪽)**: "왜 턴에 큐를 쓰나? 캐릭터마다 속도가 다르면 단순 큐로 충분한가 — 우선순위 큐와 비교."
> **솔직한 답**: "현재는 모든 적이 턴당 1회(Fast만 2회)라 리스트 순회로 충분합니다. 속도가 더 다양해지면 우선순위 큐(힙)로 다음 행동자를 O(log N)에 뽑는 구조로 확장할 수 있습니다." — *과장하지 말고 현재 구조를 정확히 설명하는 게 면접에서 더 좋게 평가됩니다.*

---

# 4. Item Inventory (인벤토리)

**자료구조: 리스트 (고정 용량)**

### 5단계 답변

1. **무엇** — 플레이어 인벤토리를 리스트로 관리합니다. 최대 10칸([player.py:19](player.py#L19)).
2. **왜** — 인벤토리는 "슬롯 번호로 빠르게 꺼내 쓰기"가 핵심입니다. 숫자키 1~0이 곧 인덱스라, 리스트의 O(1) 인덱스 접근과 정확히 맞습니다.
3. **어디** — `player.py`. 저장은 `self.inventory`([player.py:18](player.py#L18)), 추가는 적 처치 드롭(`attack` 내부, [player.py:65-66](player.py#L65-L66)), 사용은 인덱스로 꺼내는 `use_item`([player.py:95](player.py#L95)). 아이템 종류는 `item.py`의 `Weapon`/`Potion`([item.py:43-59](item.py#L43-L59)).
4. **대안 비교** —
   - **딕셔너리(이름→개수)**: "포션 몇 개?" 같은 *이름 기반 조회*가 잦으면 O(1)이라 유리. 하지만 우리는 슬롯 번호로 꺼내므로 리스트가 더 자연스럽습니다.
   - **트리(BST)**: 아이템을 *정렬된 상태로 유지*하거나 범위 검색이 필요할 때 빛납니다(검색/삽입 O(log n)). 10칸짜리 작은 인벤토리엔 과합니다 — 리스트 선형 탐색도 최대 10번이라 충분히 빠릅니다.
5. **복잡도** — 인덱스 접근/사용 O(1). 특정 아이템 검색은 O(n)이지만 n≤10이라 무의미. 딕셔너리면 이름 검색 O(1), 트리면 정렬·검색 O(log n).

> **예상 질문 (PDF 7쪽)**: "인벤토리에 List vs Dict vs Tree? 트리가 검색·정렬에서 이기는 건 언제?"
> **답 요지**: 슬롯 기반 소규모 인벤토리라 리스트. 이름 조회가 잦으면 Dict(O(1)), 정렬 유지·범위 검색이 필요하면 Tree(O(log n))가 유리해지는 임계점을 설명.

---

# 5. Enemy AI (적 AI)

**알고리즘: BFS(추적) + Bresenham 직선(시야)**

### 5단계 답변

1. **무엇** — 적이 플레이어를 추적할 때 **BFS**로 최단 경로의 다음 한 칸을 구합니다. 시야 판정은 **Bresenham 직선 알고리즘**으로 벽이 가리는지 확인합니다.
2. **왜** — 던전 격자는 **간선 가중치가 모두 동일**(어느 칸으로든 1칸 = 비용 1)합니다. 가중치 없는 그래프의 최단 경로는 **BFS가 정확히 보장**하면서 가장 단순합니다.
3. **어디** — `enemy.py`. BFS 한 칸 이동은 `_bfs_next_step`([enemy.py:104](enemy.py#L104)), 시야의 직선 그리기는 `_bresenham`([enemy.py:5](enemy.py#L5))과 `can_see`([enemy.py:76](enemy.py#L76)). 적 종류별 행동은 각 `update`(Melee [196](enemy.py#L196), Ranged [220](enemy.py#L220), Fast [243](enemy.py#L243), Boss [284](enemy.py#L284)).
4. **대안 비교** —
   - **DFS**: 길을 찾긴 하지만 *최단*을 보장하지 않습니다. 적이 빙 둘러 가는 비합리적 추적이 됩니다.
   - **A***: 휴리스틱(맨해튼 거리)으로 BFS보다 *탐색 노드 수*를 줄일 수 있습니다. 맵이 매우 크거나 적이 많아 성능이 문제될 때 유리. 우리 맵(80×50, 시야 6~8칸 반경)에선 BFS로 충분히 가볍고, A*는 구현 복잡도만 늘립니다.
   - **Dijkstra**: 간선 가중치가 다를 때 필요. 우리는 전부 1이라 Dijkstra는 BFS로 퇴화 — 굳이 쓸 이유 없음.
5. **복잡도** — BFS는 정점 V, 간선 E에 대해 O(V+E). 격자에선 V=칸 수, E≈4V라 사실상 O(칸 수). 단, 적은 **시야 안에서만** 추적하므로 실제 탐색 범위는 시야 반경으로 제한됩니다.

> **예상 질문 (PDF 7쪽)**: "적이 플레이어를 어떻게 추적하나? BFS냐 A*냐 — 여기선 뭐가 더 맞고 왜?"
> **답 요지**: 무가중치 격자라 BFS가 최단을 보장하면서 가장 단순. A*는 맵·적이 커질 때의 최적화 카드. DFS는 최단 보장 안 됨.

---

# 6. Leaderboard (리더보드)

**알고리즘: 복합 키(tuple key) 정렬 — 클리어 우선 → 점수 → 시간**

### 5단계 답변

1. **무엇** — 플레이어별 최고 기록(점수·클리어 여부·소요 시간)을 저장하고, 조회 시 **세 가지 기준의 복합 키로 한 번에 정렬**해 반환합니다. 우선순위: ① 클리어한 기록이 위 → ② 점수 내림차순 → ③ (동점이면) 소요 시간 빠른 순.
2. **왜** — 순위가 점수 하나로는 안 정해지기 때문입니다. "보스를 잡고 깬 사람"이 "못 깼지만 점수만 높은 사람"보다 위여야 하고, 점수까지 같으면 빠르게 깬 쪽이 위여야 합니다. 이건 **여러 키로 정렬하는 전형적인 문제**라, 각 기준을 한 튜플로 묶어 `sorted`에 넘기면 파이썬이 튜플을 앞에서부터 차례로 비교해 정확히 이 우선순위를 구현합니다.
3. **어디** — `leaderboard.py`. 레코드 구조는 `{"score", "cleared", "time"}`. 정렬 키는 `_rank_key`([leaderboard.py:7](leaderboard.py#L7))가 `(not cleared, -score, time)` 튜플을 만들고, `get_leaderboard`([leaderboard.py:57](leaderboard.py#L57))가 그 키로 `sorted`. 같은 이름은 더 좋은 기록만 남기는 갱신을 `add_score`([leaderboard.py:45](leaderboard.py#L45))가 같은 키로 판단. JSON 영속화는 `load`/`save`([leaderboard.py:26-55](leaderboard.py#L26-L55)). 클리어 여부·시간은 게임 종료 시 `main.game_over_screen`이 `add_score(name, score, cleared=won, time=elapsed_sec)`로 전달.
   - **키 변환 트릭**: `sorted`는 기본이 오름차순이라 "큰 게 위"인 점수는 `-score`로, "True가 위"인 클리어는 `not cleared`(False=0이 먼저)로 부호를 뒤집습니다. 시간은 "작은 게 위"라 그대로.
   - **점수 공식** (`render.compute_score`, [render.py:29-32](render.py#L29-L32)): killXP×100 + gold×500 − Undo횟수×10 − 경과초×10, 최소 0. (gold는 현재 항상 0.) 누적 killXP는 `main.turn_update`에서 매 턴 계산.
4. **대안 비교** —
   - **점수 하나로만 정렬**: 단순하지만 클리어/시간을 못 반영 — 못 깬 고득점이 깬 사람 위로 와 순위가 직관에 어긋납니다.
   - **여러 번 나눠 정렬**: "시간으로 정렬 → 점수로 정렬 → 클리어로 정렬"처럼 단계별로 할 수도 있지만(파이썬 sort가 안정 정렬이라 가능), 복합 키 튜플 한 번이 더 명확하고 실수가 적습니다.
   - **힙(top-k)**: "상위 10명만" 필요하면 크기 k 힙으로 O(n log k) — n이 매우 크고 k가 작을 때 전체 정렬보다 빠릅니다. 우리처럼 기록 수가 적으면 차이가 미미하고, 전체 정렬이 코드가 더 단순합니다. (힙에 넣을 때도 같은 복합 키를 비교 기준으로 쓰면 됩니다.)
5. **복잡도** — 전체 정렬 O(n log n), 공간 O(n). 복합 키 튜플 비교는 상수배만 늘 뿐 차수는 그대로. top-k 힙이면 O(n log k). 추가/갱신은 딕셔너리라 O(1).

> **예상 질문 (PDF 7쪽)**: "점수를 어떻게 정렬하나? 상위 10명만 필요하면 전체 정렬이 정말 필요한가 — 힙과 비교."
> **답 요지**: 점수 하나가 아니라 **(클리어, 점수, 시간) 복합 키**로 정렬해 순위를 유일하게 결정. 전체 순위 표시라 `sorted` O(n log n). top-k만 필요하면 힙 O(n log k)가 이론상 유리하지만 기록 수가 작아 실익이 적음.
> **추가 예상 질문**: "왜 점수 하나가 아니라 튜플로 정렬하나?" → 순위 기준이 여러 개라 단일 값으로는 표현 불가. 튜플은 앞 요소부터 비교되므로 우선순위를 그대로 인코딩할 수 있음.

---

# 제출 전 체크리스트 (PDF 10쪽)

- [ ] AI+X Studio 컴퓨터에서 게임을 돌려봤다 (우리 노트북 말고)
- [ ] 시작 → 게임오버까지 크래시 없이 돌아간다
- [ ] 6개 핵심 기능이 코드에 전부 있다
- [ ] 각 기능에 쓴 자료구조/알고리즘 이름을 댈 수 있다
- [ ] 각 알고리즘을 최소 1개 대안과 비교할 수 있다
- [ ] 각각의 시간/공간 복잡도를 말할 수 있다
- [ ] PPT가 코드가 실제로 하는 일과 일치한다
- [ ] 각 기능이 구현된 **파일·함수 이름**을 안다
- [ ] 팀원 각자가 예상 질문에 대한 답을 연습했다

---

# 한 줄 요약 (PDF 11쪽)

> **Run it. Defend it. Compare it.**
> 게임이 *무엇을* 하는지가 아니라, *왜 그렇게 만들었는지* 를 보여줄 것.

| 기능 | 자료구조/알고리즘 | 핵심 대안 |
|------|------------------|-----------|
| Dungeon Map | 2D 그리드 | 그래프(인접 리스트) |
| Undo | 스택 (LIFO) | 큐(FIFO) / full-state vs delta |
| Turn | 순차 순회 | 우선순위 큐(속도 다를 때) |
| Inventory | 리스트 (슬롯) | Dict(이름조회) / Tree(정렬검색) |
| Enemy AI | BFS + Bresenham | DFS(최단X) / A*(대형맵) |
| Leaderboard | 복합 키 정렬(클리어→점수→시간) O(n log n) | 점수 단일 정렬 / 힙 top-k O(n log k) |
