# Dungeon Map 설계 문서

## 자료구조

**2D List** `list[list[int]]` 사용

```python
# 타일 값
0 = 바닥
1 = 벽
2 = 문 (미래 확장)
```

```python
map_grid[y][x]  # 접근 방식
```

## 생성 방식

매 게임(층)마다 절차적으로 새로 생성.

### 알고리즘: Random Room Placement + L자 복도

1. 고정 크기 그리드를 전부 벽(1)으로 초기화
2. 랜덤 위치·크기의 직사각형 방을 N개 배치 — 겹치는 방은 스킵 (AABB 충돌 검사)
3. **모든 방 연결** — 방 중심 좌표 기준 순차 L자 복도로 고립 방 없도록 보장
4. **시작/도착 지점 배치** — 전체 방 목록 중 중심 간 맨해튼 거리가 가장 먼 두 방 선택, 각각 시작(플레이어 스폰)·도착(계단/출구)으로 지정. 최소 거리 미달 시 맵 재생성
5. 나머지 방에 몬스터·아이템 랜덤 배치

### 시작/도착 거리 보장

- 모든 방 쌍의 맨해튼 거리 계산 후 최대값 선택
- 최소 거리 threshold(`MIN_DIST`) 미달 시 맵 재생성

### 방 연결 (고립 방 방지)

- 방 목록을 순서대로 순회하며 인접한 두 방을 L자 복도로 연결
- 이렇게 하면 모든 방이 하나의 연결 그래프에 속하게 됨

### 몬스터·아이템 배치

- 시작 방과 도착 방에는 배치 안 함
- 나머지 방마다 확률적으로 몬스터 0~2마리, 아이템 0~1개 배치
- 층(floor)이 깊을수록 몬스터 수·강도 증가

### 파라미터 (조정 가능)

| 파라미터 | 기본값 | 설명 |
|---------|--------|------|
| `MAP_W` | 80 | 그리드 가로 타일 수 |
| `MAP_H` | 50 | 그리드 세로 타일 수 |
| `ROOM_MIN` | 4 | 방 최소 크기 |
| `ROOM_MAX` | 10 | 방 최대 크기 |
| `ROOM_COUNT` | 12 | 생성 시도 횟수 |
| `MIN_DIST` | 30 | 시작·도착 방 최소 맨해튼 거리 |

## 편집 방법

```python
# 타일 읽기
tile = map.grid[y][x]

# 타일 쓰기
map.grid[y][x] = 0  # 0=바닥, 1=벽

# 범위 체크
map.in_bounds(x, y)  # → bool

# 통행 가능 여부
map.is_walkable(x, y)  # → grid[y][x] == 0
```

## TODO

- [ ] `Map.__init__` — 그리드 초기화 + 생성 호출
- [ ] `Map._place_rooms` — 방 배치 (AABB 충돌 검사)
- [ ] `Map._connect_rooms` — 순차 L자 복도 연결
- [ ] `Map._place_start_end` — 거리 최대화된 시작/도착 방 선정
- [ ] `Map._spawn_entities` — 몬스터·아이템 배치
- [ ] `Map.in_bounds` / `Map.is_walkable` — 유틸
- [ ] `render()` 연동 — 타일별 색상 또는 스프라이트
